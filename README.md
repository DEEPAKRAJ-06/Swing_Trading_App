# Swing Trader Cloud

Streamlit + Postgres cloud version of the local Swing Trading App.

Run locally from this folder:

```powershell
pip install -r requirements.txt
streamlit run streamlit_app.py
```

For hosted deployment, follow `README_DEPLOY.md`.

This is educational trading research only. It does not place broker orders.
